<?xml version="1.0" encoding="utf-8"?>
<!-- M-13: Block cleartext traffic except localhost (bridge IPC on port 8765/8766).
     Trust only system CAs — no user-installed CA certificates in production. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- AndroidBridge (8765) and stats server (8766) use HTTP on localhost -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
