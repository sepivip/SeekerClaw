# BAT-1155 — xAI OAuth token durability (single-writer cross-process store + expiry round-trip) + revoked-token UX

> **Severity:** High. The always-on agent on xAI "Sign in with Grok" (OAuth) **bricks** when a restart reloads an already-consumed refresh token: xAI's single-use reuse-detection revokes the whole token family, and every subsequent restart re-loads the same dead token. Recovery needs a manual re-pair (or switching to an API key). Live device incident (2026-07-13, agent "Cortana", grok-4.5).
>
> **Status:** v4 — design **Codex-signed**; **implemented** in PR #443. Codex's subsequent CODE review returned 3 blockers + 2 majors, all folded (see §9). Awaiting Codex re-review + the 24h device soak (§6.9) before merge. v4 design amendments over v3: (blocker 1) §4 is marker-ONLY, sign-out preserves the marker, no token-presence heuristic; (blocker 2) D6 pins exact budgets (300/1200/≤1700/250+2000/2500 ms) and a controlled-stop durability gate instead of a blind kill on drain failure; (edit 1) D-recover is restart-only; (edit 2) the sidecar OS lock is wrapped by same-process serialization.

---

## 1. Incident — the proven fact

From the device `node_debug.log` (root-caused via a 4-investigator + adversarial-verify pass):

- The agent ran healthy ~2 days on xAI OAuth, rotating the refresh token cleanly **9 times** at the ~6h cadence; heartbeats `200` up to **12:12**.
- After a **USER_STOP → restart**, the first refresh on that boot (~13:10) returned `HTTP 400 "Refresh token has been revoked"`. Every request since 403s; the agent shows `"session expired — re-pair"` (Telegram) and, per-turn, the **wrong** `"add an xAI API key"`.
- **The revocation is the load-bearing proof:** a token freshly minted at the last good rotation (09:42) is unused and could never return "revoked". So the boot POSTed an **already-consumed** refresh token. There is **no** `[xAI] OAuth token persist attempt N failed` line — the stale token reached disk via a **write that reverted it**, not a failed persist.

**Minimal proven fact:** `config.json` at the doomed boot carried an already-consumed refresh token. The exact staling *writer* is not observable from `node_debug.log` alone (needs MAIN-process logcat around 09:42–12:40) — but two code-verified hazards both produce it, and the fix must close **both**.

---

## 2. Root cause + the dominant contributing defect

### 2a. Cross-process token clobber (durability root)
`seekerclaw_prefs` is opened `MODE_PRIVATE` (`ConfigManager.kt:307-308`) — **no** cross-process coherence. The rotation-owned keys `KEY_XAI_OAUTH_TOKEN_ENC / REFRESH_ENC / EXPIRES_AT` are written by **two processes**: `:node` on each rotation (`persistXaiOAuthTokens`, `ConfigManager.kt:1789-1856`) AND **MAIN/UI** on any full-config save (`saveConfig`, `605-624`), plus the `loadConfig` reconcile write-back (`1437-1451`). A MAIN `editor.commit()` **flushes that process's entire stale in-memory prefs map to disk** — reverting the `:node`-rotated token to an older, consumed pair, no error either side. `CrossProcessStore.kt`'s own header names this exact class: *"SharedPreferences … credentials are all latent instances of the same [staleness] bug."* This is that instance surfacing.

> The reconcile write-back (`1438-1440`) puts only `KEY_PROVIDER/AUTH_TYPE/MODEL` — but its `commit()` at `1451` still flushes the whole stale map, so it is a clobber vector via whole-map flush, not via the keys. This is why the fix is to **move the tokens out of `seekerclaw_prefs` entirely**, not to edit which keys any editor puts.

### 2b. Expiry never round-trips → boot-churn (the dominant real-world amplifier)
The log proves `_currentExpiresAtMs` seeds to **0 on essentially every boot**: a **14-minute-old** token (mint 15:22:53 → boot → refresh 15:36:17, both `200`, `node_debug.log:28891-28892 / 28971-28972`) still fired a boot-time proactive refresh. So the **D5 opportunistic refresh fires on the first use after every restart, consuming + rotating the single-use token each boot.** With **123 starts / 66 stops**, the token was rotated **60+ extra times purely by boots** — this churn is what makes any clobber/kill-window fatal.
> **Correct citation:** MAIN `saveConfig` writes `KEY_XAI_OAUTH_EXPIRES_AT` **unconditionally** (`ConfigManager.kt:622`) from the MAIN `AppConfig` snapshot; the `isNotBlank` guard is in `writeConfigJson` (`~2114`). An unconditional overwrite from a stale/blank MAIN snapshot is a *stronger* clobber than a guarded one — it can blank the expiry outright — which is why the persisted expiry never reaches Node's boot seed.

### 2c. Secondary bugs (confirmed, independently valid)
- **Misclassification (user-facing):** terminal 403 branch (`xai.js:335-340`) returns `type:'provisioning'` + *"add an xAI API key"* with **no `_refreshDead` guard** — dressing a revoked OAuth token as a subscription/tier gate, contradicting the `re-pair` Telegram (`ai.js:1974`).
- **Dead-token recognition is one-error-code-fragile:** `xai.js:504` latches `_refreshDead` only on `parsed.error === 'invalid_grant'`. (It DID latch here — D10 reached 3 strikes, which requires `refreshDead===true`.)
- **Restart re-notify loop:** the revoked token is never marked dead **on disk** (persist runs only on a *successful* rotation), so each restart re-seeds it, resets `_refreshDead=false`, and re-runs the doomed cycle (three "session marked expired" events across restarts: `node_debug.log:27619 / 31213 / 31352`).
- **Grace-retry** (`xai.js:328`) is not gated on `!_refreshDead`.

### 2d. Explicitly NOT a bug (do not change)
D10 session-expiry counting (`ai.js:1963-1965`): a dead/revoked token (`refreshDead=true` → `isFreshTierGate=false`) correctly counts toward re-pair; a fresh-token tier-gate is correctly excluded. Verified correct by all three lenses.

---

## 3. Design decisions

### D1 — Move the xAI OAuth record to a dedicated single-domain `CrossProcessStore` (durability root fix)
Migrate the full record `{ accessToken, refreshToken, expiresAt, email, reauthRequired, reauthNotifiedEpoch, epoch, tombstone }` out of `seekerclaw_prefs` into a **dedicated `CrossProcessStore<XaiOAuthTokens>`**, reusing the canonical atomic pattern (`.tmp` + `Files.move(REPLACE_EXISTING, ATOMIC_MOVE)`; `read()` reads fresh from disk every call — no cached map; FileObserver/broadcast reload).

- **Location — `files/xai_oauth.json` (applicationContext.filesDir root), a SIBLING of `runtime_state.json`/`bridge_token`, explicitly NOT under `workspace/`.** `CrossProcessStore` roots at `filesDir` and `isValidFileName` rejects `/` (`CrossProcessStore.kt:161, 766-771`), so a `workspace/…` path is literally unconstructible AND unsafe: the agent's file tools are `safePath`-confined to `workDir=files/workspace` (`config.js:13`), so a workspace-resident store would be **readable** (leaks base64 ciphertext + expiry + `reauthRequired`) and **deletable** (one tool call → `read()` falls back to empty → tokens vanish → forced re-pair) by a prompt-injected agent. `filesDir` keeps it outside `safePath` exactly like `runtime_state.json` today, and avoids a second FileObserver on the workspace dir (one-observer-per-dir rule).
- **Defense-in-depth:** add `xai_oauth.json` to `SECRETS_BLOCKED` (`config.js:768`) so the agent `read`/`write`/`delete` tools refuse it even if `workDir` is ever relocated.
- **Secrets at rest:** `accessToken`/`refreshToken`/`email` are stored as **KeystoreHelper AES-256-GCM ciphertext (base64, `IV||ciphertext`)** inside the JSON — identical key/format to today's prefs, so migration is a **ciphertext passthrough** (no cleartext ever materializes). `expiresAt`, `reauthRequired`, `epoch` are plaintext (non-secret). **Integrity of the plaintext control fields is a security property** — the `filesDir` location (out of agent write-reach) is what protects them from prompt-injection tampering (a forged `reauthRequired=false` or far-future `expiresAt` would re-open the churn/brick).
- **Writers (exactly two, both legitimately own the token):** (1) `:node` rotation via the existing `/xai/oauth/save-tokens` bridge (now writes the store, not prefs); (2) MAIN sign-in/sign-out (`XaiOAuthActivity` + Settings). `saveConfig` / `loadConfig` reconcile **never touch these keys again** → the whole-map-flush clobber is structurally impossible (an editor that doesn't own the key can't flush it).
- **Reader — SINGLE SOURCE OF TRUTH:** **every** reader of the xAI OAuth record sources it from the store, not prefs: `loadConfig` (`ConfigManager.kt:1211-1302`), `writeConfigJson` (which reads via `loadConfig`, `2039→2111`), the readiness/health surface (`xaiOAuthSet`, `2211-2233`), `updateConfigField` (`1920-1923`), and the Settings sign-in-state UI. **No divergent prefs copy survives migration** — otherwise `loadConfig` returns a stale/blank token while rotations only hit the store, and `writeConfigJson` emits a consumed/missing token → the live `"Missing required config (xaiApiKey or xaiOAuthToken)"` crash (`config.js:627-636`; `node_debug.log:26276-26291`).
- **Node’s decrypted surface is unchanged:** Node still receives decrypted tokens only via the ephemeral `config.json` (deleted ~5s after boot); it never reads the store directly (it can't decrypt Keystore blobs). Value-level `registerRedactedSecret` for the decrypted access/refresh tokens stays as-is (`main.js:64-65`). **There is no `redactor` "file-path/no-log" primitive** — do NOT prescribe one; confidentiality = Keystore ciphertext, reachability protection = `filesDir` + `SECRETS_BLOCKED`.

**Concurrency — cross-process CAS under a sidecar OS lock (Codex amendment 1; epoch-alone is NOT sufficient).** `CrossProcessStore` guarantees only last-writer-wins across processes (`CrossProcessStore.kt:386, 521-523`; `update{}` is same-process-only), so two independent RMWs merely comparing an `epoch` still have a TOCTOU window: MAIN and `:node` both read epoch N, MAIN writes a fresh sign-in, and `:node`'s stale rotation still lands **after**. Node's rotation payload also carries no epoch today (`xai.js:529-535`) and the bridge reconstructs a merge after `loadConfig` (`AndroidBridge.kt:977-985`). **Required protocol** — every token-store mutation (rotation, sign-in, sign-out, mark-reauth, migration) runs under a **stable sidecar OS advisory file lock** (a *separate* inode, e.g. `files/xai_oauth.lock`, that the atomic-rename never replaces) as one transaction: `acquire lock → read current record → validate expectedEpoch → write an epoch-advanced record via the existing tmp+ATOMIC_MOVE → release`. Both writers execute **Kotlin** (the `:node` write runs in the bridge handler in the `:node` process; MAIN sign-in in the UI process), so a `java.nio FileChannel.lock()` on the sidecar gives real cross-process mutual exclusion — no service funnel that could be stopped. **Same-process serialization (Codex consistency edit):** wrap the sidecar `FileChannel.lock()` in an in-JVM mutex/monitor per process — a Java `FileLock` is held per-JVM, not per-thread, so two threads in one process could otherwise race into overlapping acquisition (`OverlappingFileLockException`). The in-JVM mutex serializes same-process callers; the sidecar file lock then covers the *other* process. Semantics:
  - **every** mutation advances the revision (`epoch`) — **EXCEPT annotation-only writes** (`markReauth`, `markReauthNotified`), which keep the *same* family alive and therefore hold `epoch` stable while CAS'ing on it, so a caller can chain a mark + notify on one epoch and a concurrent sign-in still supersedes both. Rotation, sign-in, sign-out, and migration all advance `epoch`;
  - `epoch` is emitted to Node (store → `writeConfigJson` → `config.json` → `config.js` → `xai.js` tracks `_currentEpoch`); each rotation persist sends its **`expectedEpoch`**;
  - under the lock, **reject with a distinct conflict code if the on-disk `epoch` differs** from `expectedEpoch`;
  - on conflict, Node **discards/clears that stale pending pair and restarts/reloads — it MUST NOT retry the pair later** (retrying a stale pair is what reuses a consumed token);
  - **sign-out writes an epoch-advanced tombstone** (dead record) so an in-flight old rotation (lower `expectedEpoch`) is rejected and cannot resurrect the account;
  - **migration is fill-only** (no-op if any token already present), also under the lock.

  The CAS conflict code + Node's discard-not-retry rule is what makes this safe — the `epoch` value alone does not. This plumbing (Node payload `epoch`/`expectedEpoch`, the new conflict path, the tombstone) must be explicit in the implementation and the concurrency tests.

### D2 — Round-trip the expiry so healthy boots skip the D5 refresh
With `expiresAt` in the D1 store (never blanked by an unrelated `saveConfig`), `writeConfigJson` always emits the last-rotation expiry into `config.json`, and `config.js:276 → xai.js:120` seeds `_currentExpiresAtMs`. **Acceptance:** a boot with a still-valid (future) expiry fires **zero** refresh POSTs on first use; only an expiredish or unknown (`0`) expiry triggers the D5 one-shot. Removes the boot-churn.

### D3 — `_refreshDead`-aware terminal 403 classification (UX fix)
Before the terminal `provisioning` return (`xai.js:335-340`):
```javascript
if (isOAuth && _refreshDead) return { type: 'reauth', retryable: false,
    userMessage: 'Your Grok sign-in was revoked — reconnect xAI in Settings.' };
```
Keep *"add an xAI API key"* ONLY for a genuine fresh-token tier-gate (`_refreshDead === false`). `reauth` is `retryable:false` (skips the retry gate `ai.js:1844`) and still counts toward re-pair (D10: `isFreshTierGate` stays false). **D3 depends on D7** (below) or the first dead-token 403 is preempted.

### D4 — Persisted `reauthRequired` flag with a FULL round-trip (stops the re-POST / re-notify loop)
- **Write path (token-less):** a **new dedicated bridge endpoint `/xai/oauth/mark-reauth`** sets `reauthRequired=true` in the D1 store **without touching the token fields** — idempotent, and **exempt from / on a separate bucket than** the `5/60s` `/xai/oauth/save-tokens` rate limit (a dead-token heartbeat storm must not be throttled). The existing save-tokens path can't carry it (it requires an `accessToken`, `AndroidBridge.kt:973`, and a dead token has none).
- **Read path (must be plumbed — mirror the expiry):** store → `writeConfigJson` emits `reauthRequired` into `config.json` → `config.js` exports it → `xai.js` seeds skip-refresh/`_refreshDead` from it **on boot**. Without this, boot starts `_refreshDead=false` and re-runs the doomed cycle — D4 is inert.
- **Behavior when set:** skip **all** refresh POSTs and do not run the rotation cycle.
- **"Notify once" without silencing the user (Codex amendment 3; reserve-before-send per Codex re-review):** persist a **revision-scoped `reauthNotifiedEpoch`** field in the D1 record (NOT an ambiguous global `notified` marker). The autonomous notice is **RESERVE-before-send**: `xai.js noteReauthNotified()` durably CAS-reserves the dead epoch, and `ai.js` sends the single autonomous notice **only if the reserve lands**. The product invariant is therefore: **at most one autonomous *send attempt* per dead epoch, durably reserved before the send** — a failed reserve sends nothing and is retried on a later dead turn / next boot (so there is **no duplicate**); a CAS conflict (family superseded) sends nothing; and **delivery may be OMITTED after a reservation success followed by a crash / Telegram failure** (accepted trade-off). Regardless, **(b) every explicit user request** while `reauthRequired=true` STILL gets the actionable reconnect response (never silent); **(c) no D4+D10 duplicate** for the same event (see D-recover); **(d) one copy everywhere**: *"Your Grok sign-in was revoked — reconnect xAI in Settings."* `reauthNotifiedEpoch` is set on a durable RESERVE (before the send attempt) and cleared on sign-in.
- **Self-clear:** the dead flag is cleared only by a successful re-pair/sign-in (the sign-in store write clears `reauthRequired` + `reauthNotifiedEpoch` **in the same atomic, epoch-advanced snapshot**). Keep `xai.js:453`'s clear as belt-and-suspenders for the non-dead path (it can't fire in the dead path since D4 suppresses the refresh that would reach it).

### D5 — Broaden dead-token detection **narrowly** + deterministic logging
At `xai.js:504`, latch `err.reLogin` on `parsed.error === 'invalid_grant'` (primary) **OR** a specific description match `/revoked|already been used|token expired/i` on `parsed.error_description` for an HTTP 400 from the refresh endpoint. **Never** match the bare `refresh[_ ]?token` substring (it appears in benign transient 400s like *"refresh_token parameter is required"* → a single false match would persist `reauthRequired` and permanently brick a working account, since D4 then suppresses the refresh that would self-clear it). Only latch on a death **observed on an actual refresh attempt**, never inferred. Add a one-line **redacted** log of `parsed.error` on refresh failure so xAI's exact revoke code becomes deterministic.

### D6 — Drain the pending token persist on USER_STOP: single-flight + exact budgets (Codex amendment 4)
- **Single-flight (must-have):** `_attemptPersist` has no in-flight guard today (`xai.js:544-546`), so a USER_STOP flush racing the original rotation persist can POST the **same payload twice** — and with the D1 CAS that second write becomes a **false conflict**. Add a `_persistInFlight` single-flight and export a **bounded `flushPendingPersist`**: `await` the existing in-flight attempt first, then make **at most one** forced retry — but **only for a transport/timeout failure that leaves a pair genuinely still-pending**. A CAS **epoch conflict** clears the pending pair (per D1: discard-not-retry) so it is no longer pending and is **never** re-POSTed by this forced retry — re-POSTing a consumed pair is exactly the brick this whole contract prevents.
- **Ordering (pinned):** run the token drain **BEFORE** the session-summary flush (not the vague "first and/or parallel" of v2) — token durability outranks a summary.
- **Budgets — PINNED (one closed inequality, Codex blocker 2):**
  - `flushPendingPersist`: **300 ms hard bound as ONE end-to-end deadline** shared across both phases (the await of an in-flight attempt AND the single forced retry) — not 300 ms allocated to each — with the remaining budget passed explicitly to `androidBridgeCall` (whose default is 10 000 ms at `bridge.js:31`);
  - session summary: **1200 ms** (unchanged from today);
  - Node `/shutdown/flush` endpoint total target: **≤ 1700 ms** (300 drain + 1200 summary + DB/response overhead);
  - `NodeControlClient`: connect **250 ms**, read **2000 ms**;
  - `SeekerClawService` outer bound: **2500 ms** (250 + 2000 + 250 margin) — i.e. **widen** the current `withTimeoutOrNull(2000ms)` at `706-712` to 2500 ms so the outer cap cannot preempt the (drain-first) sequence. Bypass the `5/60s` rate limit for the shutdown write. (Equivalent numbers are acceptable, but the inequality is now closed, not deferred.)
- **Controlled-stop failure path (Codex blocker 2) — a timed-out/failed drain must NOT be followed by a blind kill.** If a rotated pair is still unpersisted after the single forced attempt, the server may already have consumed T0 → minted T1 while disk holds T0; an unconditional `killProcess` then guarantees the next boot reloads the consumed T0. On the **user-initiated Stop** path, gate the kill: **(preferred)** abort/delay the controlled stop, keep the service alive, surface a concise *"finishing secure session save…"* state, and retry — safest when the token store itself is having I/O trouble; **(fallback)** before kill, durably write a **fail-closed reauth/tombstone** state (via the same locked store write) that guarantees the old refresh token is never POSTed on boot. The unconditional `onDestroy` kill remains only as the OS/final fallback (it can't be avoided when the OS reclaims the process), but the Stop button path must pass through this pre-stop durability gate.
- Closes the kill-window where xAI consumed the old token (`xai.js:433`) before the new one is persisted (`xai.js:459`).

### D7 — Gate grace-retry on `!_refreshDead` (REQUIRED, not cosmetic)
Add `!_refreshDead` to the guard at `xai.js:328` (`!_oauth403RetriedOnce && !_tierGated`). Without it, on a dead-token boot the **first** 403 hits the grace-retry (returns `provisioning`/`retrying`) and only the **second** reaches D3's reauth message — re-showing the wrong copy once per boot in the exact incident scenario. **D3 + D7 are interdependent.**

### D-recover — Reconcile remediation surfaces + in-place recovery
- **One message:** on a reauth boot, D4's `reauth` surface and D10's `ai.js:1974` "session has expired — re-pair" must not both fire. The autonomous notice is **at most one send attempt per dead epoch** (reserve-before-send; delivery may be omitted after a reserve-then-crash) and, **if delivered**, uses the single consistent remediation copy on both the per-turn and Telegram surfaces (reconnect/re-pair — never "add an API key"). Every explicit user request always gets that copy.
- **In-place sign-in recovery:** a Settings "Sign in with Grok" writes the store but does NOT reload Node's in-memory `_refreshDead` (Node reads `config.json` only at boot; `repairReset` at `xai.js:686` *assumes* a full setup → service restart). Per **locked decision 5**, a successful sign-in **triggers a `:node` restart** (the single recovery path — no bridge hot-reload alternative) that clears `_refreshDead`/`reauthRequired`; D2 makes that restart zero-refresh while the TTL is valid, reusing the established boot/H5 path.

---

## 4. Upgrade & downgrade safety
- **Durable migration tombstone/marker, independent of successfully decoding the token record (Codex amendment 2):**
  - **marker absent:** run the migration under the D1 cross-process lock — read legacy prefs, ciphertext-passthrough into the store, **verify the store write, then atomically set the marker**.
  - **marker present:** the dedicated store (incl. a sign-out **tombstone**) is the **only** source of truth, **forever — no token-presence heuristic and no legacy fallback**. Store corruption / decrypt failure **FAILS CLOSED to reauth** (surface reconnect, set `reauthRequired`) — it **must NEVER emit a legacy token**. (Critical: a post-migration legacy fallback would replay an already-consumed refresh token — the exact incident — because after the first store-backed rotation the legacy prefs token is consumed.)
  - **Gate on the marker ALONE — NEVER on a "store has no token AND legacy prefs has a token" heuristic, and never on file presence (Codex blocker 1).** The no-token heuristic is unsafe after a legitimate **sign-out tombstone**: stale legacy prefs may still hold the prior refresh token, so a heuristic re-import would **resurrect the signed-out account**. **Sign-out preserves the migration marker.**
  - Run the idempotent migration gate (marker-absent branch only) in **both** processes: **MAIN before its first config load/save**, and **`:node` before the first `config.json` emit** — so no boot reads an empty store (→ api_key downgrade / "Missing required config" crash).
  - **After verified migration, remove the legacy `KEY_XAI_OAUTH_*` keys from each process's prefs view.**
- **Ciphertext passthrough:** copy the legacy base64 blobs verbatim (same Keystore key/format); `email` stays ciphertext; `expiresAt` moves as-is. The migration path and every failure branch **must never log decrypted values**.
- **Downgrade posture — DECIDED (Codex amendment 2): accept that downgrade requires re-pair; do NOT keep a prefs rollback shadow.** A shadow preserves a potentially-consumed token and restores the exact hazard on downgrade. Safer downgrade behavior is *missing credentials → explicit re-pair*, never stale-token replay.
- **Preserve H5:** the store sign-in write must retain the `runtime_state` authType re-derivation (`ConfigManager.kt:1833-1847`) that prevents the `(xai,api_key)/(xai,oauth)` boot-loop.

## 5. Scope — OpenAI Codex OAuth deferred (with justification)
`openai.js` persists via `/openai/oauth/save-tokens` into the same `MODE_PRIVATE` prefs — the same clobber (2a) exposure. **But OpenAI has no proactive-refresh hook** (`writeConfigJson:2104-2107`), so it does **not** boot-churn (2b) — materially lower risk than xAI. This contract fixes **xAI only** (the live incident); D1 is written generically so an OpenAI follow-up reuses the store type. **Decision (Codex):** do NOT block this fix on documented family-revocation wording — the incident already proves stale-token replay is unsafe, so keep the exact revoke code **empirical/redacted** (D5's `parsed.error` log). **OpenAI is a documented residual risk (clobber 2a only, no boot-churn) and gets a linked follow-up ticket** that reuses this store type.

---

## 6. Test plan (reusable regression-catchers)
1. **Cross-process durability (instrumentation, NOT single-process Robolectric — that gives a false green):** two processes sharing the real store; assert a MAIN full-config save can never revert a `:node`-written token; atomic-move never observed torn. *(The trivial "saveConfig no longer touches the keys" is necessary but insufficient — must exercise the real cross-process races below.)*
2. **Cross-process CAS (instrumentation):** a rotation carrying a **stale `expectedEpoch`** (a fresh sign-in landed first) → **rejected with the conflict code**; Node **discards** the stale pending pair and does **NOT** re-POST it. A **sign-out tombstone** → an in-flight old-family rotation is rejected and cannot resurrect the account.
3. **Upgrade migration (instrumentation):** install over a legacy-prefs build → token preserved; idempotent (marker-gated, both processes); a migration write that **loses a race to a concurrent rotation** no-ops (fill-only). **Corrupt-store-AFTER-migration → FAILS CLOSED to reauth, never emits a legacy token**. **Marker present + sign-out tombstone + legacy prefs token STILL present → remains signed out; ZERO migration/re-import** (blocker-1 regression). **Downgrade → re-pair** (missing credentials), never stale-token replay.
4. **`ensureFreshToken` (xai.js):** future expiry seed → **0** refresh POSTs on first use; `0` → exactly **one** D5 POST; `reauthRequired=true` (via the boot round-trip) → **0** POSTs + one `reauth` surface.
5. **`classifyError` (xai.js):** terminal 403 → `reauth`+reconnect when `_refreshDead`, tier-gate copy only when not; `err.reLogin` latches on `invalid_grant` and on `/revoked|already been used|token expired/` but **NOT** on bare "refresh token"; grace-retry (D7) suppressed when dead so the **first** 403 yields reauth.
6. **`reauthRequired` + `reauthNotifiedEpoch` boot round-trip (reserve-before-send):** store flags → `config.json` → `config.js` → `xai.js` boot gate → no refresh; **at most one autonomous *send attempt* per dead epoch, durably reserved before the send** — a failed reserve sends nothing and stays retryable (no duplicate), a CAS conflict sends nothing, and delivery may be omitted after a reserve-then-crash; yet **every explicit user request still gets the reconnect response** (never silent); no D4+D10 duplicate for the same event.
7. **Shutdown single-flight + budget + failure path:** with a rotation persist **already in flight** when USER_STOP arrives, `flushPendingPersist` does **not** double-POST the same payload (no false CAS conflict); the drain runs **before** the summary and completes within the recomputed 2500 ms ceiling. **Forced persist timeout/failure** (store I/O error): the controlled-stop **durability gate** fires — service stays alive with the *"finishing secure session save"* state (or a fail-closed reauth/tombstone is durably written) — **NOT a blind kill that leaves the consumed token on disk** (blocker-2 regression).
8. **Node integration:** boot from valid-future-expiry token → 0 refreshes; boot from consumed token → `reauth` + one re-pair message, **never** "add an xAI API key".
9. **Device soak (Seeker, xai/oauth, grok-4.5) — ACCEPTANCE:** ≥24h across **≥3** USER_STOP/restart cycles **and** ≥1 Settings save between rotations, **plus an upgrade-install leg** (install over a legacy build). Use a **deterministic force-revoke** (a second consumer of the refresh token, or admin revoke) to exercise the dead path. Verify from `node_debug.log`: (i) **BEFORE the deliberate force-revoke**, ZERO `"Refresh token has been revoked"` (no *spontaneous* revocation from clobber/churn — the whole point of the fix); (ii) every boot's `[Config]` load carries the last rotation's expiry (no proactive refresh with TTL remaining); (iii) **AFTER the force-revoke**, the dead-token path engages cleanly: **at most one autonomous reconnect send attempt per dead epoch** (reserve-before-send — delivery may be omitted after a reserve-then-crash), and **if delivered** it is the consistent reconnect/re-pair copy on both surfaces, never "add an xAI API key"; every **explicit user request** still gets the reconnect reply; (iv) an in-place Settings sign-in recovers without a manual restart.

## 7. Invariants
- D10 counting (`ai.js:1963-1965`) unchanged (§2d).
- No new plaintext-secret surface; `email` stays Keystore ciphertext; only non-secret `expiresAt`/`reauthRequired`/`epoch` are plaintext, protected by the `filesDir` location + `SECRETS_BLOCKED`.
- Rotation cadence stays ~1/6h on the happy path (D2 removes the extra boot rotations); no new refresh-storm.
- The `H5` runtime_state authType sync is preserved through the rewrite.

## 8. Decisions (locked by Codex review)
1. **Dedicated `XaiOAuthTokenStore`: YES** (tokens must not land in a Node-readable plaintext file).
2. **Downgrade: forced re-pair; NO prefs shadow** (a shadow preserves a consumed token and restores the hazard).
3. **Concurrency: sidecar interprocess lock + expected-epoch CAS** — NOT epoch-only last-writer-wins (§D1 Concurrency).
4. **Provider semantics:** do not block on undocumented family-revocation wording — the incident proves stale-token replay is unsafe; keep the exact xAI code empirical/redacted (D5). OpenAI = documented residual risk → **linked follow-up** ticket.
5. **Recovery: restart `:node` after a successful sign-in** — D2 makes that restart zero-refresh while TTL is valid, and it reuses the established boot/H5 path (no new hot-reload state path).

*The four amendments are explicit and the test matrix includes their failure cases (both satisfied in v4). Codex design-signed v4; §9 folds the subsequent code-review blockers.*

---

## 9. Code-review fold (post-v4 Codex CODE review — 3 blockers + 2 majors)
Contract v4 was design-signed; Codex's subsequent **code** review returned NO SIGN-OFF with 3 blockers + 2 majors. Each is now folded (implementation-only; no D1–D9 intent change):

- **Blocker 1 — shutdown could kill without positive durability.** Added a dedicated pre-stop **`XaiOAuthDurabilityGate`** (`state/`) run OFF the main thread by `SeekerClawService.stop()`/`restart()` BEFORE `stopService()`. It (1) re-drives Node's flush up to 3 rounds so a VALID rotated `T1` persists first, (2) on unpersisted/unreachable, CAS-marks the family reauth (`Ok`/`Conflict` ⇒ durable), all under ONE monotonic (`System.nanoTime`) end-to-end budget; a non-durable result KEEPS the service alive and retries (bounded) — *"finishing secure session save"* — instead of a blind kill. `onDestroy`'s fallback now fail-closes when Node is already gone (no silent skip) and `markReauth(rec.epoch)` CAS-handles a concurrent sign-in. The once-per-stop persist routes to a dedicated **`/xai/oauth/save-tokens-urgent`** bridge bucket (20/60s) so the normal 5/60s throttle can't strand a valid token.
- **Blocker 2 — unversioned dead-marks could poison a fresh family.** `markReauth`/`markReauthNotified` are now **CAS'd on `expectedEpoch`** (epoch NOT advanced — annotations); Node sends `_currentEpoch` and, on `409`, adopts the winning epoch and clears the pending mark (retryable, never swallowed).
- **Blocker 3 — epoch-0 tombstone was resurrectable.** `rotate()` rejects EVERY tombstone regardless of epoch; the epoch-0 fill exception lives ONLY in `migrateIfEmpty`.
- **Major 1 — decrypt failure wasn't fail-closed.** `decryptXaiCiphertext` now returns `String?` (`null` = a NON-blank ciphertext failed to decrypt vs `""` = absent); `loadConfig` treats a required-token decrypt failure as effective reauth (blank tokens + `xaiOAuthReauthRequired`).
- **Major 2 — no store tests + a red suite.** Added `XaiOAuthTokenStoreTest` (16-case v4 matrix driving the REAL store via a JVM seam: fail-closed default, corruption, signIn/signOut, rotate CAS, epoch-0 + tombstone rejection, versioned marks, fill-only migration + marker semantics). Fixed the 3 stale `shutdown-flush.test.js` greps to the D6 shapes and made `CrossProcessStoreTest`'s drift-guards CRLF-tolerant. Full suite green: **433 Kotlin tests + Node offline suite**, 0 BAT-1155 failures.

## 9.1 Code-review fold (Codex RE-review of 8c0b2a44 — 2 blockers + 3 majors)
- **Blocker 2 (new brick vector) — `pendingPersist:false` hid two disk-unsafe states.** Convergence-exhausted (`_persistDurableError`: T1 discarded, consumed T0 on disk) and a failed dead-family mark (`_markReauthPending`: disk still says the family is live) both reported clean. Added an authoritative **`diskUnsafe`** signal (`isDiskUnsafe()` = pending-pair OR persistDurableError OR markReauthPending, isOAuth-gated), plumbed through `/shutdown/flush` (200 + 500 bodies) and `NodeControlClient.flushShutdown` (prefers `diskUnsafe`, falls back to `pendingPersist`). The gate keys on it.
- **Blocker 1 — controlled Stop must NEVER call `stopService` without positive durability.** Dropped the bounded-retry-then-`finishStop` "OS fallback": `stopWithDurability` calls `finishStop` ONLY in the durable arm; on exhaustion it keeps the service ALIVE with a dismiss-resistant *"finishing secure session save"* notice (only an OS force-stop bypasses the gate); a non-durable restart does not fire its start callback either.
- **Major 1 — the 2500 ms budget wasn't enforced across a late store mutation.** `markReauth(expectedEpoch, maxLockMs)` → `withStoreLock` caps the sidecar-lock acquisition to `min(LOCK_TIMEOUT_MS, remaining)`; the gate passes its remaining monotonic budget.
- **Major 2 — notify-once mark failure was swallowed.** Retained (`_noteNotifiedPending`) + retried CAS'd to its epoch; a conflict drops it.
- **Major 3 — behavioral coverage.** Node: `diskUnsafe` (convergence-exhausted / failed-mark / api_key gate) + notify-retry (success clears, conflict drops). Kotlin `XaiOAuthDurabilityGateTest`: fail-closed-on-unreachable, no-op for dead/tombstone/uninitialized, and a **lock-budget bound under real sidecar-lock contention**. A drift pin that `stopWithDurability` calls `finishStop` only in the durable arm; `diskUnsafe` emitted on both flush bodies. **Note:** this project has **no instrumentation-test framework** (established convention: JVM logic tests + device validation for Android surfaces — see `CrossProcessStoreTest` header). The literal two-OS-process MAIN-vs-`:node` sidecar-lock race is therefore validated by the **§6.9 device soak** (real MAIN + real `:node`, restarts/rotations/forced-revoke/Settings-saves) — the JVM suite now pins all the single-process decision logic + budgets. **439 Kotlin tests green; Node offline suite green (0 BAT-1155 failures).**

## 9.2 Code-review fold (Codex RE-review of 468a9073 — 1 blocker + 3 majors)
- **Blocker — no quiesce barrier between the "disk safe" ack and the kill.** The gate proved durability at one instant, but a heartbeat/turn could rotate `T0→T1` AFTER the ack and before the kill → stranded pair. Added a process-wide quiesce latch (`quiesce.js`): `/shutdown/flush` **quiesces first** (refuses new turns via `message-handler.js`, heartbeats via `main.js`, and token ROTATIONS via `xai.js refreshOAuthToken`), drains, and stays quiesced until teardown. An abandoned Stop (kept alive) calls the new `/unquiesce` (`NodeControlClient.unquiesce`) to resume.
- **Major 1 — HTTP round not truly bounded.** `withTimeoutOrNull` can't interrupt a blocking `HttpURLConnection`; `flushShutdown(maxTotalMs)` now caps the underlying connect/read timeouts themselves, and the gate passes its remaining budget. Proven by a stalling-loopback wall-time test.
- **Major 2 — notify marker lost on immediate Stop.** A dead family made the gate return durable without contacting Node, so a failed notify mark followed by an immediate Stop re-notified on restart. `flushPendingPersist` now **drains the pending notify mark** within budget and reports separate `notifyPending` metadata (not `diskUnsafe` — a re-notify is not a brick).
- **Major 3 — deterministic two-process harness (Codex-approved over `androidTest`).** `XaiTwoProcessHelper` + `XaiTwoProcessRaceTest` spawn a real SECOND JVM (ProcessBuilder, `-cp` via `@argfile`) contending on the same files through the production sidecar-lock + CAS: MAIN signIn vs stale rotate; signOut-tombstone vs rotate; stale markReauth vs fresh signIn; migrate fill-only vs rotation; simultaneous barrier-released signIns serialize (no lost write); a held OS lock forces a bounded sibling failure. The §6.9 device soak remains the Android integration/acceptance layer. **448 Kotlin tests green (two-process 7/7, budget 2/2); Node suite green.**

## 9.3 Code-review fold (Codex RE-review of 58bc4914 — 3 majors; token-brick blocker verified closed)
- **Major 1 — the gate ignored `notifyPending`.** `flushShutdown` now returns a structured `FlushResult(diskUnsafe, notifyPending)`. *(Superseded by §9.4 — see below: the notice guarantee is now enforced Node-side by reserve-before-send, so the gate does NOT block on notify metadata; `notifyPending` is reported for observability only.)*

## 9.4 Code-review fold (Codex RE-review of e4e4980b — 1 blocker + 2 majors)
- **Blocker — the auto-expiring lease reopened the clean-ack → kill race.** A 15s lease could expire during a delayed main-looper handoff from the durability proof to `onDestroy`/kill, letting Node resume and rotate. Fixed with a **lease renewer** (`SeekerClawService.startLeaseRenewal`): from the instant durability is proven until kill, an INDEPENDENT daemon thread POSTs the new `/quiesce` endpoint every 5s (≪ 15s lease), so quiescence stays airtight across the handoff. It self-terminates once `:node` stops answering (killed) or after an iteration cap, and is cancelled at every lifecycle entry (`start`/`stop`/`restart`) so a restart's renewer can never re-quiesce the fresh `:node`. On abandon, renewal is cancelled + `/unquiesce` retried, with lease expiry as the backstop.
- **Major 1 — the notify guarantee must be enforced, not redefined.** Replaced the "residual proceeds" gate behavior with **reserve-before-send** (Codex's recommended option): `noteReauthNotified()` now durably RESERVES the dead epoch (CAS) and ai.js sends the ONE autonomous notice ONLY if the reserve lands. A failed reserve → the notice is NOT sent (retried on a later dead turn / next boot — nothing was sent, so **no duplicate**); a CAS conflict (family superseded) → no notice due. This makes D4's "at most one autonomous notice per dead epoch" hold with **zero duplicates** (a crash after reserve may OMIT the notice — accepted, since explicit user requests still get reconnect copy). The gate + Node shutdown drain therefore no longer touch notify metadata, and the "at most one duplicate" language is removed.
- **Major 2 — held-lock harness could hang before its timeout.** The lock-holder now signals readiness via a FILE; the parent polls it with its own timeout (and drains stdout on a background thread), so a helper that hangs before acquiring is force-killed and FAILS the test instead of blocking the runner on a `readLine()`.
- **Major 2 — abandoned Stop could freeze the agent forever.** `quiesce` is now a **lease** (monotonic `hrtime`, `LEASE_MS`) that auto-expires if not refreshed, so a kept-alive agent self-resumes even if every `/unquiesce` is lost; the abandon path also retries unquiesce with bounded backoff (`unquiesceUntilConfirmed`). Tested: lease expiry/refresh; unquiesce returns `false` (not a throw) on a dead endpoint.
- **Major 3 — harness hardened.** `run()`/`collect()` drain stdout on a background thread so `waitFor(30s)` runs independently → a hung child FAILS (force-killed), never hangs the runner. Competing ops now fire GENUINELY concurrently via ready+go barriers, and the two Codex named as missing are added: stale-`markReauthNotified`-vs-signIn and `migrateIfEmpty`-vs-rotation-on-empty-store (plus the sequential adopt-tombstone-epoch-then-rotate proof). **454 Kotlin tests green (two-process 8/8, gate 9/9); Node xai + shutdown-flush + quiesce green.**

## 9.5 Code-review fold (Codex RE-review of a9c10a36 — 1 blocker + 1 major; + CodeRabbit)
- **Blocker — the renewer stopped before kill.** It exited on 2 consecutive POST failures or a 24-iteration (120s) cap — neither proves the process was killed, so a transient outage or a long handoff could still let the lease lapse. Now **lifecycle-bound**: renewal runs until an EXPLICIT `stopLeaseRenewal()` (or a newer lifecycle) or process death (daemon thread), ignoring POST failures; a **generation token** (not a reusable boolean) prevents a stale renewer from rejoining a newer lifecycle. Behavioral regression added (`LeaseRenewalTest`): survives a failure window, runs past the former 24-iter cap, stops only on explicit cancel.
- **Major — align the core contract with reserve-before-send.** D4 and §6 test-plan item 6 (above) are updated to the real invariant: **at most one autonomous *send attempt* per dead epoch, durably reserved before the send; delivery may be omitted after a reserve-then-crash; every explicit user request always gets reconnect guidance.**
- **CodeRabbit:** single-flight guard on `noteReauthNotified()` (concurrent turns can't both send); the two-JVM harness bounded-waits use `System.nanoTime()` (monotonic); the lease-renewal drift check is scoped to the durable arm (behavior covered by `LeaseRenewalTest`).

## 9.6 Soak-brick fix — decouple the durability answer from the session summary (device soak, 2026-07-14)

**Symptom (on-device):** a fresh, valid "Sign in with Grok" reverted to "Sign in with Grok" / "credential missing" within seconds, repeatably. The store showed the exact `markReauth` signature: `reauthRequired:true` on a live token, epoch unchanged, ciphertext preserved.

**Root cause — the durability signal was blocked behind the best-effort summary.** D6 bolted the fast xAI drain (`_xaiFlush`, ~300ms, authoritative `diskUnsafe`) onto the FRONT of `/shutdown/flush`, but the HTTP **response** still waited for `flushShutdown('USER_STOP', {summaryTimeoutMs:1200})`. The sign-in restart (`XaiOAuthActivity` → `SeekerClawService.restart`) runs the pre-stop **durability gate**; on a churning/loaded device the summary times out and the endpoint answers past the gate's per-round read budget (`BUDGET_MS 2500` / 3 rounds vs an up-to-~1700ms endpoint). The gate reads `null`, treats it as **"Node unreachable"** (§D6 fail-closed-on-unknown), and `markReauth`s the **freshly-signed-in** family — which `ConfigManager.loadConfig` then reads as dead and blanks. Every retry re-runs the trap. The prior unit tests fed the gate a FAST fake flush, so none reproduced a Node that answers durability fine but is slow on the *summary*.

**Fix — `durabilityOnly`.** A `{"durabilityOnly":true}` body makes `/shutdown/flush` return `{ok,diskUnsafe,pendingPersist,notifyPending}` as soon as the xAI drain completes and **skip the summary entirely**. The signals are unchanged; only the *coupling* is removed.
- **Gate** (`XaiOAuthDurabilityGate`): every round now calls `flushShutdown(budget, durabilityOnly=true)` — the gate never needed the summary. A slow summary can no longer time a round out to null → no false fail-close. The xAI drain still runs, so Node still gets its chance to persist a rotated T1 (the PREFERRED "stays signed in" outcome), and a genuinely stranded pair still returns `diskUnsafe:true` → the fail-closed mark still fires when it *should*.
- **onDestroy guard** (`SeekerClawService.flushNodeBeforeProcessKill`) — **SUPERSEDED by §10:** this durabilityOnly re-probe branch was replaced when the durability decision moved entirely to durable store state. `flushNodeBeforeProcessKill` now delegates to the (store-based) `ensureDurableBeforeStop` gate (dispatched off-main via `stopExecutor` with a bounded wait) and separately runs the best-effort session summary. *(Historical:* the terminal path used to re-probe with `durabilityOnly=true` on a `null` full flush to disambiguate a slow summary from genuine unreachability.*)*
- **Genuine unreachability still fail-closes.** `durabilityOnly` is fast, so a `null` from it means Node is truly gone (crash) — the crash-mid-rotation protection is intact.

**Coverage:** behavioral proof in `tests/nodejs-project/shutdown-flush-durability-only.test.js` (durabilityOnly returns `diskUnsafe` fast and NEVER invokes the slow summary; a full flush STILL runs it); Kotlin request-contract proof in `XaiSignInRestartDurabilityTest` (the gate + `NodeControlClient` request the `durabilityOnly` path, never the summary-blocking full flush). `org.json` is stubbed in JVM unit tests, so the Kotlin response-parse (the final link) is exercised on-device, not in-JVM. Logcat-visible `Log.w` instrumentation added at every `markReauth` decision (gate null-branch, onDestroy guard) for device triage.

*Any change here re-touches auth/shutdown/store/durability semantics → requires Codex re-review before merge.*

## 10. Stop-fence durability protocol (device soak, 2026-07-14) — supersedes the reachability-gated design

The §9.6 `durabilityOnly` fix was necessary but NOT sufficient: the sign-in restart can hit **genuine** `:node` control-plane unreachability (mid-kill/restart), and the gate/onDestroy guard fail-closed a **fresh** sign-in on that null probe — a false brick. Two Codex-rejected intermediates (a family-property `freshSignIn`, then a one-shot exact-epoch marker) proved that *any* control-plane-dependent decision is racy on the null branch (consume-vs-persist). **Codex-approved final design (Linear BAT-1155 threads):** decide durability entirely on **durable store state**.

### Two store fields (`XaiOAuthTokens`, `Long = -1L`, armed ≡ `field == epoch` → self-disarms on any epoch advance)
- `rotationInFlightEpoch` — a refresh POST for this epoch's on-disk refresh token has been initiated → potentially-consumed → feeds `isDiskUnsafe`.
- `stopFenceEpoch` — a controlled stop is in progress for this epoch → no refresh may begin.

`signInRestartEpoch` was **dropped** (Codex-approved): the fence, being positive safety evidence, subsumes the sign-in-restart exemption *and* fixes the pre-existing "healthy rotated family false-bricks on an unreachable stop." Upgrade-safe (`ignoreUnknownKeys` + `encodeDefaults`): a pre-upgrade record decodes both fields to `-1` (never spuriously armed).

### `prepareRefresh(expectedEpoch)` — the single atomic pre-POST gate (`:node` funnels every rotation through it, before the T0 POST). Priority order under the sidecar lock:
1. epoch mismatch → `Conflict` (superseded; Node discards, **does NOT adopt** the winner's epoch);
2. tombstone/reauth → `Dead` (terminal, no POST);
3. `stopFenceEpoch == epoch` → `Fenced` (no POST; keep the in-mem token);
4. **`rotationInFlightEpoch == epoch` (already armed) → `Unsafe`** (a prior POST may have consumed the token; a second POST is forbidden — Codex blocker) — Node latches this;
5. else arm `rotationInFlightEpoch = epoch` → `Ok`, POST may begin. At most one concurrent/cross-process `prepareRefresh(E)` returns `Ok`.

### The stop side (`armStopFenceAndProbeRotation`) ⇄ the refresh side (`prepareRefresh`) are single sidecar-locked mutations, so they serialize:
- **prepare wins** → `rotationInFlightEpoch=E` → the stop's arm/probe sees it → conditional fail-close;
- **arm wins** → `stopFenceEpoch=E`, probes `rotationInFlightEpoch==-1` → pristine → safe → the refresh sees `Fenced` → no POST.
Every interleaving is one of these — **independent of control-plane reachability**. An ungraceful kill (neither gate nor onDestroy) is still covered: the durable pre-POST marker ⇒ boot converts to reauth.

### Mutation rules (explicit; Kotlin `.copy()` inherits otherwise)
`signIn`/`signOut`/`migrate`/`FAIL_CLOSED` → both `-1`. `rotate` clears the marker and **REBASES** an armed fence forward to the new epoch (amendment 1 — a refresh authorized before the fence persisted safely must not drop the fence). `markReauth` clears the marker (durable reauth supersedes it). `markReauthNotified` **PRESERVES** the marker (amendment 3 — the notify path must never erase the safety marker before a durable reauth lands).

### Gate + onDestroy (amendment 2 — the fence is *positive* safety in both paths)
`ensureDurableBeforeStop`: not-live → durable; else `armStopFenceAndProbeRotation` (retries to fence the winning live epoch on a concurrent advance) → `rotationInFlightEpoch != epoch` ⇒ **positively safe, return true even on a null control path**; `rotationInFlightEpoch == epoch` ⇒ drain via a `durabilityOnly` flush (the store is authoritative, re-read after each round) then a **conditional** `markReauthIfRotationInFlight` (marks ONLY if still armed — a proven-not-sent clear returns `Safe`, never bricking a now-safe family — Codex major). onDestroy delegates to the same gate (unified).

**Abandoned stop (decision 3's abort half — `resolveAbandonedStop`).** The gate arms `stopFenceEpoch=E` as its FIRST action. If durability is UNCONFIRMED and the caller keeps `:node` ALIVE (does not kill), the armed fence MUST be **cleared** before the process resumes — otherwise the resumed refresh funnels through `prepareRefresh` → `Fenced` → `'skip'` (no POST, no reconnect) and xAI refresh is **silently dead until a full process restart**. `stopWithDurability`'s keep-alive-exhausted branch calls `resolveAbandonedStop`: (1) best-effort convert a still-armed marker to a durable reauth, then (2) `clearStopFence`. It returns `true` **only when the fence is durably cleared** — a reauth mark alone is NOT sufficient (CodeRabbit round-2): a still-in-flight rotation can complete via `rotate()`, which clears `reauthRequired` AND rebases the still-armed fence forward, re-bricking a live family. **Resume only behind a cleared fence.** If the clear fails, do NOT resume: retry `stopWithDurability` (bounded by `MAX_STOP_ATTEMPTS`, keeping `:node` quiesced) — a marked reauth makes the retry's gate see a not-live family → durable → a **clean kill** (boot then blanks the token via `loadConfig` + clears the fence via `reconcileXaiOAuthOnBoot`, so the fence is irrelevant to a token-less reauth family). Only a store-I/O failure past the retry ceiling resumes fenced as the last resort (the ~15s quiesce-lease lapse would resume it anyway; boot-recoverable once the disk recovers).

### Boot (amendment 3) — `SeekerClawService.reconcileXaiOAuthOnBoot`, BEFORE `writeConfigJson`/provider activation
Read store → if a rotation marker is armed, durably CAS-convert it to `reauthRequired` → clear a stale stop fence. `ConfigManager.loadConfig` additionally blanks the emitted token when the marker is armed (fail-safe belt for a conversion that could not persist). `markReauthNotified` must never clear the marker.

### Codex's 5 locked decisions
1. Marker clear is conservative — only a **provably-not-sent** transport error (`ECONNREFUSED/ENOTFOUND/EAI_AGAIN` before write). 2. A 5xx / timeout / any received response is **potentially consumed** → retain the marker, never retry from the same disk token. 3. **No fence TTL** — explicit durable clear on abort/boot. 4. `/shutdown/flush` does NOT mutate the store. 5. A single atomic `prepareRefresh`; `Fenced`/`Conflict`/`Dead`/`Unsafe`/`Failed` all = no external POST; `Conflict` never adopts.

### Coverage (Codex's ~20 required regressions)
`XaiOAuthTokenStoreTest` (prepareRefresh order incl. already-armed→`Unsafe`, rotate-rebase, `markReauthIfRotationInFlight` Ok-vs-`Safe`, `markReauthNotified` preserves, upgrade→-1), `XaiOAuthDurabilityGateTest` (fenced+no-in-flight = safe on the null path; in-flight → conditional fail-close), `XaiSignInRestartDurabilityTest` (a fresh sign-in resolves from the durable store with NO Node round-trip), `xai.test.js` (Ok→POST; Fenced/Conflict/Unsafe/Dead→no POST), `XaiTwoProcessRaceTest` (prepareRefresh ⇄ armStopFence complete-state disjunction across two real JVMs). The final Kotlin↔Node HTTP link is device-exercised (org.json stubbed in-JVM). **Follow-up (coupled, diagnostics-only — NOT a correctness gate):** a live `tests/live/xai` probe of xAI's actual revoke response to broaden the reLogin latch.

*Touches auth/shutdown/store/durability semantics → requires Codex diff re-review before merge.*

## 10.1 Adversarial self-review fold (pre-CodeRabbit, 2026-07-15)

A 4-lens adversarial self-review of the stop-fence diff (`5471e341..b3e6aac8`) surfaced 6 CONFIRMED findings + 6 minors. Folded on the same branch:

- **M1 (MAJOR, real regression introduced by the diff) — abandoned stop resumed `:node` without clearing the armed fence → silent xAI brick until restart.** Fixed with `resolveAbandonedStop` (see §10 *Abandoned stop*): marker→durable-reauth + `clearStopFence` before resume, gated on success. Regressions: `XaiOAuthDurabilityGateTest` (`resolveAbandonedStop` clears the fence + converts an armed marker to reauth so the resumed `prepareRefresh` returns `Dead`, never `Fenced`; a marker-less fence still clears; no-op on a dead family).
- **M2 (MAJOR) — durable persist-error strands the armed marker → the next expiry force-reconnects a live, unconsumed in-memory `T1`.** **Decision: conservative fail-close is KEPT** (the reconnect is safe — no family brick — and its precondition is narrow: MAX consecutive durable bridge-write failures after a successful rotation POST, then survival to expiry). The misleading `xai.js` comment that promised a "future rotation re-persists" (impossible once the marker blocks a fresh rotation) was rewritten to state the real behavior. A **retain-and-re-converge** variant (keep the pending pair + bounded re-persist until the bridge recovers, avoiding the re-pair) would touch the Codex-signed convergence contract → **deferred to Codex diff re-review, not folded autonomously** (per the "contract → Codex sign-off → implement" workflow).
- **M3/M4/M5 (test-adequacy MAJORs) — the most protocol-critical invariants shipped GREEN-on-regression.** Added to `xai.test.js`: (M3) marker-clear discipline — an erroring-socket mock (`ECONNREFUSED/ENOTFOUND/EAI_AGAIN` → marker CLEARED; timeout/`ECONNRESET`/5xx/invalid_grant → marker RETAINED); (M4) `testPrepareRefreshGate` now keys on `refreshDead` so `skip` (Fenced/Conflict, family live) vs `dead` (Unsafe/Dead, reconnect latched) can't be confused; (M5) asserts `Conflict` does NOT adopt the winner's epoch and `Ok` DOES.
- **Minors folded:** `handleUnauthorized` now treats the new `null` (gate `'skip'`) return as a NON-retryable no-op (was logged as a successful refresh → burned `MAX_RETRIES` against an unchanged token); `_prepareRefreshGate` clears an orphaned marker on an AMBIGUOUS bridge response (a lost prepare-refresh RESPONSE where the arm may have landed but no POST followed) and the dead `try/catch` was made explicit; two store tests hardened (`signIn resets both markers` now arms first; a new test drives `armStopFenceAndProbeRotation`'s retry-to-winning-epoch CAS conflict).
- **Deferred to Codex / device suite (flagged, not folded):** the M2 retain-and-re-converge optimization; scoping `ConfigManager.loadConfig`'s `rotationUnsafe` belt to the boot/emit path (it currently fires on any runtime read during the normal in-flight-refresh window — a self-healing, fail-*closed*-direction false positive, safe to leave); Robolectric coverage of the `loadConfig` belt + `reconcileXaiOAuthOnBoot` (currently device-suite-only).

### CodeRabbit round-2 fold (on the M1 fix)
- **MAJOR — `resolveAbandonedStop` must not report safe-to-resume on a reauth mark alone.** The first M1 fix returned `true` when *either* the fence was cleared *or* a reauth was marked. But a still-in-flight rotation can complete via `rotate()`, which clears `reauthRequired` **and** rebases the still-armed fence forward (`stopFenceEpoch=epoch+1`) — leaving a live family behind an armed fence → `Fenced→skip` again. Fixed: `resolveAbandonedStop` returns `true` **only** when `clearStopFence` succeeds; the service resumes only then, else retries the stop (bounded by `MAX_STOP_ATTEMPTS`) — a marked reauth makes the retry resolve as a durable clean kill (a token-less reauth family is immune to the fence). Regression: `XaiOAuthDurabilityGateTest` asserts `false` + fence-still-armed when the sidecar lock is held (clear can't land).
- **MINOR — marker-clear discipline test now asserts a POST was actually issued** (`_httpsCallCount === 1`) for every case, so a retained-marker scenario can't false-pass by failing *before* the token request.
- **Verified GO (3-lens adversarial pass).** No residual silent-*revocation* brick (a fenced resume returns `'skip'` and never presents the token → fail-closed, not a family revoke) and no unbounded loop (`MAX_STOP_ATTEMPTS`, strictly-incrementing `attempt`, off-main). **Flagged hardening (Codex, non-blocking):** on the store-I/O-broken retry path the ~15s quiesce lease can lapse before the retries finish (each gate attempt can take up to `BUDGET_MS`), so `:node` may self-resume *fenced* mid-retry — fail-closed + boot-recoverable (same terminal state as the last-resort branch), but the retry branch could actively re-quiesce each attempt to match its stated "stays quiesced" intent.
