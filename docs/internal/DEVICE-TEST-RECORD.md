# Device-Test Record

> **Why this file exists.** Device tests are run on real hardware (Solana Seeker) but the
> result often lives only in a chat session or a PR thread. Release-readiness audits read
> the *repo*, so an untracked pass looks identical to "never tested" — and gets re-flagged
> every release. This file is the durable record. **If you device-test something, add a row.**
>
> Scope: on-device verification only. CI results live in GitHub Actions; unit/integration
> results live in the PR body.

| Date | Change | Build / commit | What was exercised | Result |
|------|--------|----------------|--------------------|--------|
| 2026-08-31 | **BAT-1283** auto-resume attributed the wrong goal — re-test after the Codex diff review (#450) | `630a9c59` (SHA **verified by APK hash**, not the banner — see note) | **Solana Seeker.** Three legs. Supersedes the `74112d69` run, which predates all three review fixes. **(1) The fix.** Greeting `Hey girl` (`src=turn goalLen=8 goalFp=b4e375d0` — the original incident's fingerprint), then a multi-tool task (`goalLen=91 goalFp=323dc935`). Checkpoint on disk held the TASK plus `goalSrc:"turn"` and zero false `goalScanUnsafe` marks. After force-stop + restart: `[Goal] src=carried goalFp=323dc935`; Telegram quoted the task. `b4e375d0` never entered the resume path. **(2) Legacy checkpoint** (`originalGoal:"Hey girl"`, NO `goalSrc`, real task in the slice): **no `>` hint in Telegram** and `[Resume] … goalLen=0 goalFp=-` — no `ORIGINAL USER REQUEST` injected — while `[AutoResume] … goalFp=b4e375d0` proves the checkpoint really held the bad value. `[Goal] src=scan goalLen=61` then recovered the real task from the restored slice and answered it correctly. **(3) UPGRADE PATH** (the Codex round-2 blocker, and the reason for this re-test): a pre-fix checkpoint whose slice held redacted-but-**unmarked** text (`ask-***`) with clean, eligible chatter (`Hey girl`) sitting BEHIND it. Result `[Goal] src=none goalLen=0` — the scan **terminated** at the tainted message instead of reaching past it. On the previous build this same fixture yields `src=scan goalLen=8 goalFp=b4e375d0`, i.e. the original bug via the upgrade path. **74 lines on the final boot, 0 ERROR / 0 UNCAUGHT / 0 FATAL.** Fixtures removed afterwards. **Note — the banner SHA was WRONG:** it reported `a60107b1` while running `630a9c59`. Identity was established instead by hashing the asset inside the installed APK (`turn-goal.js` CR-normalised md5 `b5aa4f73…`, matching `630a9c59` and not `a60107b1`'s `6f5f9dee…`). Root cause filed as BAT-1293 — `providers.exec` captures the SHA at configuration time with no declared input, so a cached configuration replays the previous commit's SHA. **Also note:** Test 1 attempt 1 was killed 0.8 s after `[Goal] src=carried` by an external force-stop (`am_kill … from pid 10520`, exit-info `subreason=21 FORCE STOP`) — an artefact of the test setup, not a product fault; attempt 2 completed the full chain. It did consume 1 of only 2 auto-resume attempts. | PASS |
| 2026-08-31 | **BAT-1283** auto-resume attributed the wrong goal (#450) | `74112d69` (SHA verified in the device `=== SESSION … build=74112d69 ===` banner) | **Solana Seeker.** Two legs, both required by the Codex contract sign-off. **(1) The fix.** Reproduced the exact 2026-08-24 conditions: greeting `Hey girl` (`[Goal] src=turn goalLen=8 goalFp=b4e375d0` — the *same* fingerprint as the original incident), then a multi-tool task (`goalLen=91 goalFp=323dc935`), force-stop mid-task, restart. The checkpoint on disk carried the **task** text plus `goalSrc:"turn"`; on resume `[AutoResume] RESUMING … goalFp=323dc935` and `[Goal] src=carried` — `b4e375d0` never appears. Telegram quoted the task, where on 2026-08-24 the same setup quoted `> Hey girl`. Resumed turn completed correctly (wallet summary + live SOL price). **(2) Legacy checkpoint** (Codex-mandated, BOTH surfaces). No natural pre-fix checkpoint survived on the device, so one was written to disk in exactly the pre-fix shape — `originalGoal:"Hey girl"`, **no** `goalSrc`, a real task in the `conversationSlice`. On restart: **no `> goal` hint in Telegram** AND `[Resume] Injected system prompt resume directive … goalLen=0 goalFp=-` — i.e. **no `ORIGINAL USER REQUEST` injected at all**, while `[AutoResume] … goalFp=b4e375d0` proves the checkpoint genuinely held the bad value. `[Goal] src=scan goalLen=61` shows it then recovered real context from the restored slice and stamped the NEW checkpoint with proper provenance — the contract's best-effort fallback. **112 lines this boot, 0 ERROR / 0 UNCAUGHT / 0 FATAL.** Fixture removed afterwards. **Note:** the age gate fired twice mid-test (`SKIP … age=361s > 300s`) — correct behaviour, not a defect; the 5-minute auto-resume window expired during the operator round-trips and the fixture timestamp was reset. | PASS |
| 2026-08-24 | **BAT-1247** UX polish + log-share security fix (#449) | `c8159ec7` (SHA verified on the System screen: `2.2.0 (23) · c8159ec7`, and in the `=== SESSION … build=c8159ec7 ===` banner) | **Solana Seeker, Android 16.** Six legs. (1) **Auto-resume** — the blocker this PR introduced and repaired: force-stopped mid-task, restarted; `[AutoResume] RESUMING … attempt=1/2` then **3 API rounds (status 200)** and a correct answer delivered. Zero `ReferenceError`, zero `Startup scan failed` — pre-fix this path threw at the `goalHint` line, was swallowed by the outer catch, and burned both `AUTO_RESUME_MAX_ATTEMPTS`. (2) **Log-share scrub** (the feature) — a 4-line message containing a pasted `[database]` block with a fake credential was flattened to ONE wire record (`… ⏎ [database] ⏎ host=…`), and the Share export rendered `Message: [redacted, 108 chars]` — **exactly** the length predicted by running the real `LogShareSanitizer` patterns on JBR 21 beforehand. Leak scan across the full export: `hunter2`, `internal.db`, `[database]`, `sk-ant-api03`, `admin:` — **all absent**. A July-21 export of the same greeting (`Message: Hey girl`, unredacted) is the before/after pair. (3) **`[Resume]` goalLen/goalFp** and (4) **`js_eval`** flatten — both confirmed in the same export. (5) **Cron** — job created with a bracketed name, fired on schedule, delivered; `[Cron] Created job` + `[Cron] Executing job` both exercised. (6) **UX sweep, all 9 screens** driven over ADB: Dashboard (status *words* + dots), Logs (per-level chip colours, red Clear console, shared search, honest `110/300 entries · 190 filtered`), Skills list (actionable `⚠` / `Missing: DUNE_API_KEY` diagnostics) and detail (standard Back header replacing the red '← Skills'), Settings (**live** `OpenAI · GPT-5.6 Sol · OAuth`), System (heatmap month+weekday axes, scoped `6,214 requests · last 26 weeks`, mono-scoped version), Provider (**green radios**, Sign Out as danger-outline), Wallet (monospace truncated addresses, Disconnect as danger button), MCP + Env Vars (shared empty states; **Env Vars security warning preserved verbatim**). **Current boot: 76 lines, 0 ERROR / 0 UNCAUGHT / 0 FATAL**; only pre-existing cosmetic skill-manifest WARNs. **NOT exercised on device** (unit-tested with negative controls instead, stated rather than implied): heartbeat *alert* path (every probe returned `All clear`; cannot be forced), `[MCP] Connected to` (0 servers configured), cron `Removed`/`Skipping` sites (`deleteAfterRun` splices inline at cron.js:718 and never reaches the `remove()` log), and `flattenForLog`'s newline collapsing at the cron site (a job name cannot easily be given a newline via chat). **Note:** an 'Agent Unresponsive' scare mid-run was self-inflicted — the ADB harness's `launch()` force-stops before starting (`am_kill … from pid 8101`); recovered via Stop → Deploy. Not a product defect. | PASS |
| 2026-08-17 | **BAT-1187** target Android 16 / API 36 (#448) — **backward-compatibility leg** | `ce70ef9c` (SHA verified in device `=== SESSION … build=ce70ef9c ===`) | **Solana Saga, Android 14 / SDK 34 — the `minSdk` floor — `PAGE_SIZE=4096` (4 KB device).** The two backward-compat claims proven empirically rather than argued: (1) an app **targeting API 36 runs on API 34** (Android behaviour changes are double-gated on target *and* OS version, so older devices are unaffected); (2) a **16 KB-aligned `.so` loads on a 4 KB-page device** (larger alignment is a superset). Pulled the **installed** APK and confirmed `libnative-lib.so` = `0x4000` ×4 (proves the clean rebuild took, not a cached 4 KB binary); `libnode.so` still `0x1000` as scoped. Node runtime booted, 29 skills, Telegram connected, 64 tools, message round-trip `status:200`. **0 errors / uncaught / FATAL.** Only WARNs are pre-existing cosmetic skill-manifest notices. | PASS |
| 2026-08-17 | **BAT-1187** target Android 16 / API 36 (#448) — **Android 16 leg** | `ce70ef9c` | Android 16 device: full sweep incl. the **24-hour foreground-service soak** (the always-on Node agent surviving Android 16's FGS behaviour) — maintainer-reported PASS. | PASS |
| 2026-08-12 | **BAT-1186 Stage 1** — **session totals (both providers)** | `46c895f1` | Whole-session tally across the Anthropic + OpenAI legs below: **`anchorSkipped:1` on 72 rounds** (72 occasions the pre-fix role-blind trim would have deleted the in-flight instruction), **`[AnchorGuard]` 0** (structural fix held every time — the emergency net never fired), **max `nonAnchorLen` 35 ≤ cap 35**, **max `len` 36 = cap+1** (the additive-exempt signature: anchor rides free in the 36th slot), **4 `/resume` cycles survived**, **0 uncaught/FATAL**. Final OpenAI task completed after 3 chained resumes across 100+ tool rounds with `REPORT-ID` intact and a coherent 42-incident forensic report. | PASS |
| 2026-08-12 | **BAT-1186 Stage 1** anchor-preserving history trim (#446) — **OpenAI leg** | `46c895f1` | OpenAI `gpt-5.6-sol` (oauth). Organic **35-round** forensic turn (bridge-failure timeline), ran to the `maxSteps=35` budget (`reason=tool_budget_exhausted`). **`anchorSkipped:1` + `anchorExempt:1` on 17 rounds.** OpenAI batches ~4× harder than Claude — heaviest single-round trim **`removed:9`** (vs 3 on Claude), so it reached the anchor-at-head cliff in **~1 round instead of 18**: the worst-case high-`k` row from the ticket's table, where the pre-fix code would have destroyed the instruction almost immediately. `nonAnchorLen` max 35 ≤ 35. **`[AnchorGuard]` 0**, 0 errors/uncaught. Confirms the fix is provider-agnostic — as the bug was. | PASS |
| 2026-08-12 | **BAT-1186 Stage 1** anchor-preserving history trim (#446) — **Anthropic leg** | `46c895f1` (SHA verified in device `=== SESSION … build=46c895f1 ===`) | Anthropic `claude-sonnet-5` (setup-token). Organic **35-round** investigation turn (agent traced a real 62 h silent auth outage + 5 other log incidents to root cause) — ~2× the ~18-round pre-fix eviction cliff, ran to the `maxSteps=35` budget. `[History]` fired every round with `nonAnchorLen ≤ 35` (max 35, never breached); **`anchorSkipped:1` + `anchorExempt:1` on 18 consecutive rounds (18→35)** — the guard engaging exactly where the old role-blind trim deleted the instruction; anchor rode free (`len:35` = cap+1, 0 findings slots). WARN-then-DEBUG rate-limit (Codex amendment C) verified live: first skip WARN, rest DEBUG. **`[AnchorGuard]` count 0** (structural fix held; net never fired). 0 errors/uncaught. Agent never lost the task — pre-fix this shape died at step 17 with "I didn't receive any text". **Bonus — checkpoint/resume path (Codex R1-A) exercised end-to-end:** turn hit `reason=tool_budget_exhausted` at step 35 → `/resume` → `PATH=memory taskId=b9edbac3480edfc9` → resumed mid-investigation with the task intact and delivered the full report. Pre-fix, `slice(-8)` would have left the anchor outside the checkpoint window on a turn this long. | PASS |
| 2026-07-21 | **v2.2.0 RC acceptance** | `v2.2.0-rc1` (release artifact) | RC device test on the signed release build — maintainer-reported PASS. Covers the pre-tag gate for the 2.2.0 release. | PASS |
| 2026-07-20 | **BAT-1151** GPT-5.6 family (#442) | `f5b30c58` | Model picker propagation UI → agent (prefs / `agent_settings.json` / `runtime_state.json` all agreed); end-to-end on `gpt-5.6-sol` incl. multi-iteration turns, skills, `agent_pay` x402 (full 64-tool stack) | PASS |
| 2026-07-17 → 07-20 | **Soak: whole v2.2.0 tree** | `f5b30c58` (tree `418f5dbe`, identical to release tip `1d124574`) | ~2.5–3 days continuous real use: 1 boot, 0 crashes, 0 uncaught/FATAL, model held on `gpt-5.6-sol`, 0 logging faults (no redaction-error / drain error / rotation gap / forward error). Transient OpenAI 401/500/503 all self-healed via refresh/retry. | PASS |
| 2026-07-17 | **BAT-1161 P1A** logging substrate (#445) | `ff7a166f` | SESSION banner + `LEVEL\|epochMs\|message` wire format; event-time forwarding (node↔mirror lag 0); 37,101 retained legacy no-epoch lines coexisting (upgrade path); no secret leakage in mirror or Share; rotation state correct. Forced deep Doze (`deviceidle force-idle`) held 12.2 min: forward lag 1–2 coarse-clock ticks. | PASS |
| 2026-07-16 | **BAT-1172** xAI error-shape | (pre-merge branch) | Induced a real xAI 404 on device; log recorded `code=not-found msgLen=222` and the user-visible copy showed the provider's real reason — no fabricated "subscription tier / add an xAI API key" text | PASS |
| 2026-07-16 | **BAT-1155** xAI OAuth durability | (pre-merge branch) | Sign-in, Stop/Start, hard reboot, sign-out/in, live token rotation, rotation-adjacent restart; 4 restart cycles + 24h soak, 3 clean rotations | PASS — 0 bricks |
| 2026-07-06 | **BAT-1086 / 1087 / 1088** security hardening (#433) | `955991f8` | Tool-level negative-path verification on Seeker: `web_fetch` cross-origin header/body stripping, `agent_settings.json` model-facing key masking, SSRF guard across IPv4/IPv6 encodings | PASS |
| (various) | **BAT-1124** xAI Grok OAuth provider (#434) | pre-merge | Sign-in flow on device incl. a device-test-driven UX fix (foreground-on-sign-in) and `grok-4.5` default selection | PASS (dappStore flavor) |

## Known gaps at v2.2.0

- **googlePlay flavor** — BAT-1124's contract (D5) called for checking the OAuth tab on *both*
  flavors; recorded evidence is dappStore only. Covered by the RC device test.
- **Fresh install / first-run** — all rows above are in-place upgrades over an existing install.
  First-run setup from zero is exercised on the **RC artifact**, not pre-tag, by design.
